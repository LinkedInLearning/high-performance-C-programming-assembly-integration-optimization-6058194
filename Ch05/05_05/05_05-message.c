/* no need for a main() function as this code
   isn't a standalone program */
/* the STDIO.H header must be included for the
   definition of the puts() function */
#include <stdio.h>

void message(void)
{
	puts("Here I am, stuck in a loop!");
}
